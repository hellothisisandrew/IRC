package client

/*
import (
	"IRC/ClientServer/Messages"
	"fmt"
	"log"
	"net"
)

//Dial the server
func Dial() {

	conn, err := net.Dial("tcp", "127.0.0.1" + ":" + Messages.IRC_PORT)

	if err != nil {
		log.Fatal(err)
	}
	fmt.Println("closing")

	conn.Close()
}

 */