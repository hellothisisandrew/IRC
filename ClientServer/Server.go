package ClientServer


/*

//This function
func IrcListen() {
	fmt.Println(getLocalIP() + ":" + Messages.IRC_PORT)
	listener, err := net.Listen("tcp", "127.0.0.1:" + Messages.IRC_PORT) //Listen for IRC port using the messages

	if err != nil {		//If error then fatal log
		log.Fatal(err)
	}
	defer listener.Close()//Close the function

	for {
		conn, err := listener.Accept() // accept incoming connections

		if err != nil {
			log.Fatal(err)
			return
		}
		go func(c net.Conn) {
			defer c.Close()
		//PLace my code to handle here
			//buffer := make([]byte, 1024) //Buffer to hold information



		} (conn)
	}
}





*/








